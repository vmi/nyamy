<?xml version="1.0" encoding="utf-8"?>
<!--
  mruby.props - GENERATED FILE, DO NOT EDIT.

  Regenerated by tools/build_mruby.ps1 from
  scripter/mruby-build/Release/lib/libmruby.flags.mak.

  mruby passes its build-time configuration to consumers as compiler -D flags
  only; mrbconf.h is never rewritten by the build.  Public headers change shape
  with these defines, so every project linking mruby.lib must compile with the
  same set. Import this sheet instead of restating them.

  /std:c++20 is deliberately not propagated: mruby needs it only because
  enable_cxx_exception compiles its own C sources as C++.  Consumers merely
  include the headers and keep the project's own C++ standard.
-->
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemDefinitionGroup>
    <ClCompile>
      <PreprocessorDefinitions>HAVE_MRUBY_ENCODING_GEM;HAVE_MRUBY_IO_GEM;MRB_STACK_EXTEND_DOUBLING;MRB_USE_BIGINT;MRB_USE_COMPLEX;MRB_USE_CXX_EXCEPTION;MRB_USE_RATIONAL;MRB_USE_SET;MRB_USE_TASK_SCHEDULER;MRB_UTF8_STRING;%(PreprocessorDefinitions)</PreprocessorDefinitions>
      <ExceptionHandling>SyncCThrow</ExceptionHandling>
    </ClCompile>
  </ItemDefinitionGroup>
</Project>
